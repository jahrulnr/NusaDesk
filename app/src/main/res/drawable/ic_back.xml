<?xml version="1.0" encoding="utf-8"?>
<!-- Directional chevron for the System sub-page back row. Rotated 180 degrees
     it doubles as the forward affordance on the hub rows, so the screen ships
     one vector for both directions. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#00000000"
        android:pathData="M14.5,5.5 L8,12 L14.5,18.5"
        android:strokeColor="@color/ink"
        android:strokeLineCap="round"
        android:strokeWidth="2.2" />
</vector>
