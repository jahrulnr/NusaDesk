<?xml version="1.0" encoding="utf-8"?>
<!-- Regular launcher apps float directly on the desktop. The icon plate carries
     the visual identity; a permanent card border around every app made the grid
     feel like a settings dashboard instead of an app launcher. Pressed/focused
     states use a temporary tonal surface while the resting state stays clear. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="18dp" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/tile_surface_pressed" />
            <corners android:radius="18dp" />
        </shape>
    </item>
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/primary_tint" />
            <corners android:radius="18dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="18dp" />
        </shape>
    </item>
</selector>
